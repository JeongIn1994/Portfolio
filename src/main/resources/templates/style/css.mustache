<style>
    body {
        background-color: #8a8a8a;
    }
    #postDiv {
        text-align: center;
    }

    #postCenterDiv {
        display: inline-block;
        text-align: left;
        margin: 0 auto;
    }
    #buttonRow {
        margin-top: 15px;
        margin-left: 1px;
    }
    #content {word-wrap:break-word;
        word-break:break-all;
        height: auto
    }

    #loginPanel {
        border: 1px none peachpuff;
        border-radius: 15px;
        padding: 15px;
        padding-bottom: 20px;
        background-color: #DCDCA4;
    }

    #replyText {
        width: 100%;
    }
    .input {
        display: none;
    }

    .custom-button {
        background-color: #007bff;
        color: #fff;
        padding: 10px 20px;
        cursor: pointer;
    }

    .file-name {
        margin-top: 10px;
    }
    .header-body{
        background-color: #DCDCA4;
    }
    .headerbar {
        display: inline;
        padding-left: 150px;
        padding-right: 150px;
        font-size: 20px;
        color : whitesmoke;
        transition: font-size 0.5s ease;
    }
    .headerbar:hover {
        text-decoration: none;
        color: #8a8a8a;
        font-size: 25px;
    }
    #idxBody {
        background-color: #444444;
        width: 100%;
        height: 150px;
    }
</style>